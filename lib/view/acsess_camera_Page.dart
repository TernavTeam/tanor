import 'package:flutter/material.dart';

class AcseccCamera extends StatefulWidget {
  const AcseccCamera({Key? key}) : super(key: key);

  @override
  State<AcseccCamera> createState() => _AcseccCameraState();
}

class _AcseccCameraState extends State<AcseccCamera> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
